#include <stdio.h>
int main()

{
    int i,j;
    for ( i = 0,j=0; i < 5; i++,j++)
    {
        printf("%d %d\n",i,j);
    }
    //first exp is initial , sec exp is cond , third exp is incr or decr all are optional we can use 2 or more exp in all exp 
    
    return 0;
}
``