#include <stdio.h>
int main()
{
    int a;
    char b;
    float c;
    printf("the size of int is %d\n", sizeof(a));
    printf("The size of char is %d\n", sizeof(b));
    printf("The size of float is %d\n", sizeof(c));
    return 0;
}