#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a;
    printf("Enter the number you want factorial of");
    scanf("%d", &a);
    for (i = a; i > 0; i--)
    {printf("%d",i);
    
    }
    return 0;
}
